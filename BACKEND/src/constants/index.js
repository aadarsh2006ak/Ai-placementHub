const { ROLES, ALL_ROLES } = require('./roles');
const {
    APPLICATION_STATUS,
    ALL_APPLICATION_STATUSES,
    JOB_TYPE,
    ALL_JOB_TYPES,
    JOB_STATUS,
    ALL_JOB_STATUSES
} = require('./status');
const { HTTP_STATUS } = require('./httpStatus');

module.exports = {
    ROLES,
    ALL_ROLES,
    APPLICATION_STATUS,
    ALL_APPLICATION_STATUSES,
    JOB_TYPE,
    ALL_JOB_TYPES,
    JOB_STATUS,
    ALL_JOB_STATUSES,
    HTTP_STATUS
};
